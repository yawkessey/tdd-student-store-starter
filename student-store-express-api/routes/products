const express = require("express");
const router = express.Router();
const store = require("../models/store");

router.get("/:productId", (req, res, next) => {
  res.status(200).send(store.getById(req.prarams.productId));
});

module.exports = router;
